{
  "name": "cf-worker-wait-until-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Demo Cloudflare Worker showing ctx.waitUntil() patterns (cache, analytics, multi).",
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250219.0",
    "typescript": "^5.5.4",
    "wrangler": "4.33.0"
  }
}
